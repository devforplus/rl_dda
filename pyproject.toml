[project]
name = "rl-dda-game"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "devforplus", email = "devforplus@gmail.com" }
]
dependencies = [
    "pyxel>=2.3.18",
    "pipe>=2.2",
    "toolz>=1.0.0",
    "ultralytics>=8.1.2",
    "opencv-python>=4.11.0.86",
    "torch>=2.4.1",
    "torchrl>=0.5.0",
    "pyautogui>=0.9.54",
    "pillow>=10.4.0",
    "wasm>=1.2",
    "playwright>=1.48.0",
    "pytest-playwright>=0.5.2",
    "pywin32>=310; platform_system=='Windows'",
    "tensordict>=0.5.0",
    "requests>=2.32.3",
    "micropip>=0.1.0",
    "websockets>=12.0",
    "aiohttp>=3.9.0",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.game]
app_name = "VORTEXION"
app_version = "1.0"

[tool.rye]
managed = true
dev-dependencies = [
    "pytest>=8.3.5",
    "pygbag>=0.9.2",
    "tomli>=2.2.1",
]

[tool.rye.scripts]
test = { cmd = "python -m pytest", env = { PYTHONPATH = "src" } }
train_rl = { cmd = "python -m src.rl.train", env = { PYTHONPATH = "src" } }
evaluate_rl = { cmd = "python -m src.rl.evaluate", env = { PYTHONPATH = "src" } }
upload_data = { cmd = "python upload_game_data.py", env = { PYTHONPATH = "." } }
extract-config = { cmd = "python scripts/extract_config.py", env = { PYTHONPATH = "." } }
cleanup-config = { cmd = "python scripts/cleanup_config.py", env = { PYTHONPATH = "." } }
dev = { cmd = "python scripts/dev.py", env = { PYTHONPATH = "." } }
build = { cmd = "python scripts/build.py", env = { PYTHONPATH = "." } }
build-agent = { cmd = "python scripts/build_agent.py", env = { PYTHONPATH = "." } }
build-all = { chain = ["build", "build-agent"] }
serve = { cmd = "python -m http.server 5175 -d web/game", env = { PYTHONPATH = "." } }
serve-agent = { cmd = "python -m http.server 5176 -d web/agentic-game", env = { PYTHONPATH = "." } }
clean = { cmd = "python scripts/clean.py", env = { PYTHONPATH = "." } }

# CLI 도구 스크립트
cli = { cmd = "python -m scripts.cli", env = { PYTHONPATH = "." } }
install-browser = { cmd = "python -m scripts.cli install", env = { PYTHONPATH = "." } }
capture-console = { cmd = "python -m scripts.cli capture", env = { PYTHONPATH = "." } }
collect-data = { cmd = "python -m scripts.cli collect", env = { PYTHONPATH = "." } }

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src"] 
