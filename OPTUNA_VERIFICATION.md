# Optuna 하이퍼파라미터 검증 및 복원

## 🔍 문제 발견

학습이 계속 하락하는 문제의 근본 원인은 **Optuna 최적화 결과가 제대로 반영되지 않았기 때문**입니다!

## 📊 Optuna 최적화 결과 (2025-08-07 검증됨)

```json
{
  "learning_rate": 7.672115813828463e-05,
  "gamma": 0.9658767382045985,
  "gae_lambda": 0.9592342803721876,
  "clip_epsilon": 0.23713775795384281,
  "value_coef": 0.16579175341634528,
  "entropy_coef": 0.001669841290831729,
  "hidden_size": 128,
  "num_layers": 2,
  "activation": "relu",
  "grad_clip_norm": 1.5008088126812362,
  "batch_size": 64,           ⬅️ 중요!
  "num_epochs": 4
}
```

### Optuna 튜닝 성능

- **복합 점수**: 408.18
- **게임 점수**: 393.0
- **킬 수**: 3.9
- **생존 시간**: 446.6 스텝

이 파라미터는 **실제 게임에서 검증된 최적값**입니다!

---

## ❌ 잘못 적용된 값들

### 1차 시도 (학습 붕괴)
| 파라미터 | Optuna 최적값 | 1차 시도 | 결과 |
|---------|-------------|---------|-----|
| learning_rate | 7.67e-05 | **3e-4** (4배↑) | ❌ 학습 발산 |
| batch_size | 64 | **256** (4배↑) | ❌ 불안정 |
| entropy_coef | 0.00167 | **0.01** (6배↑) | ❌ 과도한 탐험 |

**결과**: 5000+ 에피소드 학습 후 성능 60-80% 감소

### 2차 시도 (과도한 보수)
| 파라미터 | Optuna 최적값 | 2차 시도 | 결과 |
|---------|-------------|---------|-----|
| learning_rate | 7.67e-05 | **5e-5** (35%↓) | ⚠️ 너무 느림 |
| batch_size | 64 | **256** (4배↑) | ❌ 여전히 큼 |
| entropy_coef | 0.00167 | **0.0015** (10%↓) | ⚠️ 약간 낮음 |

**결과**: 900+ 에피소드 학습 후에도 여전히 하락 추세

---

## ⚡ 문제의 근본 원인

### 1. Batch Size와 Learning Rate의 불균형

**효과적인 Learning Rate = LR × √(Batch Size)**

#### 1차 시도 (폭발)
```
LR: 3e-4
Batch: 256
효과적 LR = 3e-4 × √256 = 3e-4 × 16 = 4.8e-3
→ 너무 큼! 학습 폭발!
```

#### 2차 시도 (불균형)
```
LR: 5e-5
Batch: 256
효과적 LR = 5e-5 × √256 = 5e-5 × 16 = 8e-4
→ 여전히 큼! LR은 낮췄지만 Batch가 너무 큼!
```

#### Optuna 최적값 (균형)
```
LR: 7.67e-05
Batch: 64
효과적 LR = 7.67e-05 × √64 = 7.67e-05 × 8 = 6.1e-4
→ 균형 잡힌 설정!
```

### 2. Batch Size가 너무 크면 왜 문제인가?

1. **큰 배치**: 그래디언트는 정확하지만, 업데이트 크기가 커짐
2. **작은 LR + 큰 배치**: LR을 낮춰도 배치가 크면 효과적 LR이 여전히 큼
3. **불균형**: 파라미터가 서로 상쇄되지 않고 오히려 증폭됨

### 3. PPO의 특수성

PPO는 **on-policy** 알고리즘:
- 너무 큰 업데이트 → 정책이 크게 변함 → 수집된 데이터 무효화
- `clip_epsilon`으로 제한하지만, 배치가 크고 LR이 맞지 않으면 소용없음
- 결과: 학습이 불안정하고 성능 저하

---

## ✅ 복원된 설정 (Optuna 검증값)

```python
# src/rl/ppo_agent.py
learning_rate: float = 7.672115813828463e-05
entropy_coef: float = 0.001669841290831729

# src/rl/trainer.py
batch_size: int = 64
num_epochs: int = 4
```

### 예상 효과

1. **균형 잡힌 학습**: LR과 Batch Size가 조화
2. **안정적인 업데이트**: 적절한 크기의 정책 변화
3. **지속 가능한 개선**: 장기 학습에서도 성능 유지/향상
4. **검증된 성능**: Optuna로 실제 게임에서 검증됨

---

## 📈 학습 곡선 예측

### 이전 설정들 (실패)
```
성능
 │
500├─────╮           (초반 빠른 학습)
 │      ╲
400│       ╲          (중반 불안정)
 │        ╲___
300│             ╲___ (후반 붕괴)
 │                 ╲___
200│                     ╲___
 │
 └──────────────────────────────> 에피소드
   0    1000   2000   3000   4000
```

### Optuna 최적값 (예상)
```
성능
 │
500├──────────╭───────╮
 │        ╱         ╲
400│      ╱           ───────────  (안정적 유지)
 │    ╱
300│  ╱
 │╱
200│
 │
 └──────────────────────────────> 에피소드
   0    1000   2000   3000   4000
```

---

## 🎯 왜 Optuna를 신뢰해야 하는가?

### 1. 체계적인 탐색
- 50개 이상의 조합 시도
- TPE Sampler로 효율적 탐색
- 실제 게임 환경에서 검증

### 2. 복합 성능 지표
```python
composite_score = (
    performance["score"] * 0.4      # 게임 점수
    + performance["kills"] * 100 * 0.3  # 킬 수
    + performance["survival_time"] * 0.3  # 생존 시간
)
```

### 3. 검증된 결과
- 복합 점수: **408.18** (다른 시도들보다 높음)
- 생존 시간: **446.6** 스텝 (안정적)
- 킬 수: **3.9** (적절한 공격성)

### 4. 하이퍼파라미터 간 상호작용 고려
- Optuna는 파라미터 간 상호작용을 자동으로 고려
- 단순히 개별 파라미터를 조정하는 것보다 훨씬 효과적
- 예: LR과 Batch Size의 균형, Entropy와 Clip Epsilon의 조화

---

## 🚨 교훈

### 1. "더 안전한" 것이 항상 좋은 것은 아님
- LR을 낮추는 것 (5e-5)이 안전해 보이지만...
- Batch Size(256)와 불균형이면 오히려 해로움

### 2. 개별 파라미터보다 전체 균형
- 하나만 조정하면 안 됨
- LR ↓ → Batch ↓ 또는 LR ↑
- 전체적인 균형이 중요

### 3. 검증된 값을 믿어라
- Optuna가 실제 게임에서 검증한 값
- 임의로 조정하면 오히려 악화
- "내 직관"보다 "데이터"를 신뢰

### 4. Batch Size의 중요성
- Learning Rate만큼 중요
- 큰 배치 = 안정적이라는 착각
- PPO에서는 적절한 크기가 중요

---

## 📝 변경 사항

### 수정된 파일
1. `src/rl/ppo_agent.py`: LR과 Entropy를 Optuna 최적값으로 복원
2. `src/rl/trainer.py`: Batch Size를 64로 복원
3. `train_ppo_real_game.py`: 모든 메시지를 "Optuna 최적화"로 업데이트

### 커밋 메시지
```
fix(training): Optuna 최적화 결과로 완전 복원

문제:
- Batch Size가 256으로 잘못 설정 (Optuna는 64 권장)
- Learning Rate를 과도하게 낮춤 (7.67e-05 → 5e-5)
- 불균형으로 인한 지속적인 학습 붕괴

해결:
- Optuna 검증 결과 그대로 적용
- LR: 7.67e-05 (Optuna 최적값)
- Batch: 64 (Optuna 최적값)
- Entropy: 0.00167 (Optuna 최적값)

근거:
- 2025-08-07 Optuna 튜닝으로 검증됨
- 복합 점수: 408.18
- 생존 시간: 446.6 스텝
- 실제 게임 환경에서 50+ trials 수행
```

---

## 🎓 다음 단계

1. **재학습 시작**
   ```bash
   rye run python train_ppo_real_game.py --max-episodes 3000 --use-curriculum --curriculum-type step
   ```

2. **모니터링 포인트**
   - 초반 500 에피소드: 빠른 학습 확인
   - 1000-2000 에피소드: 안정적 유지 확인
   - 2000+ 에피소드: **하락 없는지** 주의 깊게 관찰

3. **성공 기준**
   - 보상이 안정적으로 유지되거나 향상
   - 생존 시간이 지속적으로 증가
   - 3000+ 에피소드에서도 성능 저하 없음

4. **추가 개선 (선택)**
   - Learning Rate Scheduler 도입
   - Early Stopping
   - Gradient Monitoring

---

**작성일**: 2025-10-28  
**버전**: 1.0  
**상태**: Optuna 검증값으로 완전 복원 ✅

